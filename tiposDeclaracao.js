  // Function declaration - Pode ser chamada em qualquer parte do código, pois o js carrgea todas functions pra depois renderizar
  function soma( x, y ) {
      return x + y
  }

  // Function expression - imprimir no console precisa ser após a criação
  const sub = function (x, y ) {
      return x - y
  }

  // Named function express => Usando as duas ao mesmo tempo - mesma coisa que a anterior se quiser imprirmir
  const multi = function multi( x, y ) {
      return x + y
  }
